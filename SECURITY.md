# 安全策略

## 支持范围

WaterClip 当前处于 `0.1.x` 预发布阶段，仅支持最新提交和最新便携版。请勿把本地服务暴露到公网或不可信局域网；默认地址为 `127.0.0.1:4174`。

## 报告漏洞

公开仓库尚未确定安全联系地址。发布前请由项目所有者补充私密报告渠道；在此之前，不要在公开 Issue 中粘贴 API Key、用户乐谱、项目包或生成图片。

报告应包含受影响版本、复现步骤、影响范围和最小化样例。请使用自行创建且不受版权限制的乐谱样例。

## 密钥与本地数据

- API Key 只随单次同源请求传给本地代理；服务端不落盘、不记录请求正文。
- 只有用户主动选择记住时，密钥才会明文保存在当前浏览器本地设置中。
- `.waterclip`、XLSX、PDF 和便携版目录都不包含 API Key。
- 不要提交真实乐谱、项目图片、`.env` 或调试转换产物。
