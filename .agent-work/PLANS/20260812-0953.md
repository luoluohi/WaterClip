# WaterClip 独立总谱窗口交互修复计划

1. 为总谱视口补充 `Shift + 滚轮` 横向滚动和鼠标中键自由拖动，并用纯函数单测锁定滚动方向与边界。
2. 让独立总谱窗口持有自己的语义框选状态，显示框选高亮；同步主窗口播放位置，同时复用主总谱的滚动、缩放和 M/S 行为。
3. 复测 `example.mscz` 在主窗口与独立窗口的布局，确认页面右侧、下方无页面级溢出空白。
4. 运行完整测试、类型检查和生产构建，更新日志后归档本计划并提交 Git。
