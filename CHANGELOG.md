# 更新日志

## 2026-08-15（便携版与开源准备）

- 新增免 npm 的 Windows 便携版构建：内含 Node.js 运行时、生产前后端、MuseScore Studio 4 CLI、第三方许可、源码可得性说明、构建清单和 SHA-256，并执行真实启动健康检查；公开包默认下载对应源码，内部候选可显式跳过但不得直接分发。
- 便携启动器通过 `%LOCALAPPDATA%` 下的版本化短路径 junction 调用 MuseScore，避免包位于中文或深层目录时 CLI 抛出 C++ 异常。
- 升级 `@fastify/static` 以修复路径穿越公告，将 ExcelJS 的 `uuid` 传递依赖提升到安全版本；官方 npm registry 生产依赖审计归零。
- 移除任意 Origin CORS，未知网页来源无法调用本机转换与代理 API；补充开源审计、安全策略、贡献指南和 MuseScore 替代/许可研究。

## 2026-08-15（导演标记谱与时间线联动）

- 新增 MuseScore 原生分页 PDF 导出；按声部与小节叠加 30% 紫色分镜带和“景别 · 镜头描述”小字，并对确有大片留白的页面设置紧凑裁切框。
- 图像生成与 LLM 改用两套独立 Base URL/API Key；旧设置自动迁移，密钥仍只保存在本机浏览器。
- 新增默认开启的故事板、播放进度与谱面联动：播放自动定位当前故事板，点击卡片跳到起始小节，谱面同步跟随。
- 设置面板按“谱面与播放 / 图像生成 / LLM”重新分区，在 1440px、1280px 与窄屏下保持无溢出布局。

## 2026-08-12（独立总谱窗口交互修复）

- 独立窗口现在保留并显示自己的框选高亮，且跟随主窗口播放位置。
- 总谱视口支持 `Shift + 滚轮` 横向滚动与鼠标中键自由拖动；主窗口和独立窗口行为一致。
- 移除独立窗口谱面四周不必要的容器留白，并压缩谱面右侧、下方的多余内边距。

## [Unreleased]

### Added

- 增加精确声部小节选区：Ctrl 追加/反选、Ctrl+D 清除、边缘自动滚动及缩放清选。
- 增加顶部悬浮小节编号轨、播放分镜紫色高亮、声部覆盖统计和多屏独立乐谱窗口。
- 增加可选 Canvas/Worker 增强渲染，以及 OpenAI-compatible LLM 拍摄描述辅助接口。
- 参考图操作改为并排的上传与剪贴板按钮；新分镜默认景别改为近景。

- 初始化 npm workspace、React/Vite 前端、Fastify 本地服务与 Git 维护规则。
- 支持 MusicXML/MXL 直读及 MuseScore Studio 4.7.4 的 MSCZ 转换。
- 集成 alphaTab 乐谱渲染、MIDI/SoundFont 播放、拖动 seek、声部 Mute/Solo 与框选坐标映射。
- 支持多声部 × 连续小节建组、重复补拍、反复遍次、2–16 格横/纵分屏及 2×2、3×3、4×4 宫格。
- 增加 DaVinci 风格故事板、高亮跟随、三大工作区折叠与窄屏堆叠布局。
- 增加 `gpt-image-2` 生成/编辑代理、参考图上传与剪贴板读取；固定 1280×720、medium、PNG。
- 增加 IndexedDB 自动保存、`.waterclip` 项目包往返和含图片的五列 XLSX 导出。
- 增加生产 SPA 静态托管、安全边界及 27 个领域、持久化、导出与服务回归测试。

### Fixed

- 修复偶发 MuseScore 非零退出但已生成有效 MusicXML 时误报 422；转换增加一次有界重试，并区分无效谱面、进程异常和超时。
- 修复按钮聚焦改变 Enter 快捷键语义、领域错误抛到控制台、乐器铭牌缩成数字和原生滚动条挤占布局的问题。

- 修复 `example.mscz` 转换成功后 alphaTab 工作线程未显示谱面的兼容问题，改用主线程解析，并在首次载入时一次性指定全部声部；同时规范横向小节宽度，避免第 6–10 小节回到负坐标并覆盖第 1–5 小节。
- 修复横向或纵向滚动后框选坐标重复叠加滚动量、释放后选框残留，以及指针取消时交互状态粘滞的问题。
- 乐谱改为单一横向连续总谱，在受限工作区内独立双向滚动；每个声部行增加随横向滚动保持可见的乐器铭牌。
- 声部监听新增由 alphaTab 实际 NoteOn/NoteOff 力度驱动的电平柱，并遵循 Mute/Solo 的可听状态。
- 修复 alphaTab 被 Vite 依赖预打包后 Worker/Worklet 地址落入不存在的 `.vite/deps`、导致播放器始终无法就绪的问题；播放请求会排队，并对不支持 Web Audio/Worker 或初始化超时给出明确提示。
- 增加逐小节编号、固定圆形当前小节标记和全部谱面乐段 A/B/C… 跳转；拖动进度条或单击小节会同步移动横向乐谱视口。
- 增加可选整页自动翻谱，翻页时保留上一页末小节；框选范围使用青色，播放位置使用朱红，避免状态混淆。
- 乐谱内每个乐器铭牌增加 M/S 控件；故事板按起始小节稳定排序，并加入 `Ctrl+Z`、`Ctrl+Shift+Z`、`Del` 工作区快捷键。
- 设置面板可填写 MuseScore 4 可执行文件路径，服务端对路径、文件名与版本进行校验且不持久化该路径。
- 降低播放位置向 React 状态发布的频率并移除超大播放线阴影区域，改善长总谱播放时的卡顿；同时约束页面级溢出，去除右侧与底部空白。
- 修复 React 回调变化导致 alphaTab 在渲染前被销毁的问题。
- 修复替换乐谱后旧分镜和选区残留导致声部 ID 错配的问题。
- 修复窄屏下大型乐谱撑宽工作区、顶部操作按钮移出视口的问题。
